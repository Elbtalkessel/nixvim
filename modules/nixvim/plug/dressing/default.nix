{
  # TODO(cleanup): dressing
  plugins.dressing = {
    enable = false;
    settings = {
      input = {
        relative = "win";
      };
      select = {
        backend = [ "telescope" ];
      };
    };
  };
}
